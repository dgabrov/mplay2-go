**STEP 1**
- create post-login endpoint, post at url /login
- expects payload of type LoginData
- see how I did the structure for root and do the same
- will come back with addl info

**STEP 2**
- add config called "context" in config json and ConfigData
- all the urls for endpoint are prefixed with context
- currently put value /mediaserv
